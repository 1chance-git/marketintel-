{
  "name": "marketintel-",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node stream_engine.js",
    "live": "node stream_engine.js --live",
    "check": "node --check supabase_client.js && node --check stream_engine.js && node --check youtube_publisher.js && node --check video_clipper.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "puppeteer": "^23.6.0"
  }
}
